spring:
  application:
    name: kafkaHW
  jackson:
    serialization:
      name: order-service
  jackson:
    serialization:
      write-dates-as-timestamps: false
  kafka:bootstrap-servers: localhost:9092
    producer:
      key-serializer:
        
server:
  port: 8080

